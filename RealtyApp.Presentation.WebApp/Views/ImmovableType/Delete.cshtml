@using RealtyApp.Core.Application.ViewModels.ImmovableAssetType;
@model ImmovableAssetTypeSaveViewModel
@{
    ViewData["Title"] = "Eliminar tipo de propiedad";
}

<div class="row">

    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>Esta seguro que desea eliminar el tipo de propiedad: <b> @Model.Name</b>?</h4>
            </div>
            <div class="card-body">

                <form method="post" asp-controller="ImmovableType" asp-action="Delete">

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="ImmovableType" asp-action="Index" class="btn btn-warning">Cancelar</a>

                </form>

            </div>
        </div>
    </div>

</div>

